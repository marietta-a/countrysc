# Release Notes — GeoLocation.Data 1.0.1

- Added `Authors` package metadata.
- Internal: extracted country loading into a `LoadCountries()` method on `GeoLocationService` (no behavior or API change).
